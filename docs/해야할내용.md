@docs/TNA_research_plan.html  파일 이름을 ET-NAGraphSAGE.html로 바꾸고 변경된 내용과 모델명을 ET-NAGraphSAGE로 수정해라. 또한 1.기존 연구 분석, 2.제안 방법론과 같은 섹션은 클릭하면 새로운 페이지에서 보여지도록 수정해라. 사이드바에 스크롤이 안보이도록 해라. 사이드바에 섹션의 하위 섹션은 접히도록 표현해라. 다른 섹션을 누를땐 이전에 눌러둔 섹션은 자동으로 접히도록 수정해라.

데이터에 대한 정보와 노드와 엣지에 사용되는 데이터도 기록해라.

현재 입력 데이터 구성을 위해 제안한 이웃 노드 선택은 어떤 방법을 사용하니?

---
우리가 제안한 기여점이 몇가지가 되니?
C1. Edge Temporal Sequence-Guided Dynamic Attention ★ 핵심

kinematic 이웃 관계의 T프레임 이력(e_ij^temp)이 동적 게이트(α_ij)를 생성하여 이웃 노드 표현을 가중한다.
GAT·EGAT가 현재 프레임 1개 → scalar 가중치인 것과 구조적으로 다름.

C2. Node + Edge 시계열 통합 인코딩 + NAGraphSAGE Edge-Aware 공간 집계 결합

두 시계열 인코더를 NAGraphSAGE 공간 메시지 패싱과 통합한 전체 파이프라인.

C3. 5단계 Ablation C (C1→C5)로 각 요소의 State_Acc 기여도 완전 분리 측정
이건 기여도가 아니고 실험이다.

---