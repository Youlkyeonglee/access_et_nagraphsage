# 학교 서버(192.168.1.11) 실행 실험 목록 (2026-07-12) — 20260712 추가 실험계획

로컬 서버(GPU 0-3)는 현재 STGCN(GPU0 자리→interleaved로 교체됨)·TGN(GPU1→position_only)·
edge-temporal(GPU2)·Transformer(GPU3)로 이미 꽉 차 있어, §실험현황 "20260712 추가 실험계획"
(6D invariant가 HiVT·QCNet에 뒤지는 격차를 좁히기 위한 4개 조치 + 조합 실험)은 이 서버에서
진행한다. 6D invariant 기준선(76.21%/74.78%)은 로컬에서 이미 완료돼 있으니 재실행 불필요.

## 0. 사전 준비

1. **코드 동기화** — 아래 파일이 반드시 최신(2026-07-12 커밋)이어야 함:
   - `models/tsem_semantic_derivation.py` — `variant='invariant_rot'`(8D: v,a,j,ω,d_lat,κ,
     cos_rot,sin_rot) + `LearnableSemanticResidual` 클래스 신설
   - `models/tsem_sage.py` — `TSEMSAGE.__init__`에 `learnable_residual_dim: int = 0` opt-in
     파라미터 추가(0이면 기존 동작과 완전 동일)
   - `models/temporal_encoder.py` — `encoder_type='transformer'` 신규 구현(2-layer
     `nn.TransformerEncoder`, sinusoidal PE, causal mask — `comparison/transformer_tsem`과
     동일 패턴)
   - `train_tsem.py` — `build_model()`이 `learnable_residual_dim`을 config에서 읽어 전달하도록
     배선
   - `modules/data_manager_tsem.py`, `modules/tsem_eval.py`, `modules/tsem_instant_label.py`
     (공통 데이터/평가 파이프라인, 변경 없음이지만 최신 상태 확인)
2. **데이터 경로 확인**: 각 config의 `data.data_dir`가 로컬 서버 경로
   (`/home/oem/data/TII_data/`)로 커밋돼 있음 — 이 서버 실제 경로(예:
   `/home/oem/yklee/data/` 등, `.claude/CLAUDE.md` "학교 서버" 항목 참조)로 로컬 수정만 하고
   **커밋하지 말 것**.
3. conda env `tna_research` (torch 2.x + CUDA). 전부 Mamba 미사용 → `mamba_ssm` 불필요.
4. **캐시**: W=10 실험 9개는 캐시가 W10/H10/radius20/K6-4 조합으로 로컬과 동일 키라 이 서버에서
   최초 1회 생성(~10-15분) 후 9개가 전부 공유한다. W=20/W=30 실험 4개는 각각 별도 캐시가
   새로 생성된다(캐시 키에 W가 포함되므로 자동 분리, 서로 안전).
5. **스모크테스트 검증 완료(로컬, 2026-07-12)**: 아래 18개 config 전부 랜덤 텐서로
   forward+backward+NaN 확인을 이미 통과했다 — 이 서버에서 별도 스모크 없이 바로 실행해도 된다.

공통 data/graph/train/loss/augment 설정은 전부 `configs/tsem_sage_invariant_6d.yaml`(6D
invariant 기준, 76.21%/74.78%)과 동일 — model 블록만(semantic_variant/hidden_dim/d_e/
encoder_type/learnable_residual_dim/W) 실험마다 다르다. 평가는 전부
`modules/tsem_eval.py::evaluate_tsem` 재사용.

---

## 1. ①②③ 단독 실험 (W=10 그대로, 캐시 공유) ★★★

```bash
git pull origin main

# ① 상대 heading 회전량 추가 (6D→8D)
CUDA_VISIBLE_DEVICES=0 python -u train_tsem.py \
  --config configs/tsem_sage_invariant_8d_rot.yaml --gpus 0 \
  > logs/tsem/train_invariant_8d_rot.log 2>&1

# ② 학습 가능한 잔차 브랜치 (speed,accel 기반 4D 추가)
CUDA_VISIBLE_DEVICES=1 python -u train_tsem.py \
  --config configs/tsem_sage_invariant_6d_learnable.yaml --gpus 1 \
  > logs/tsem/train_invariant_6d_learnable.log 2>&1

# ③ 용량 확대 (hidden_dim 128→192)
CUDA_VISIBLE_DEVICES=2 python -u train_tsem.py \
  --config configs/tsem_sage_invariant_6d_h192.yaml --gpus 2 \
  > logs/tsem/train_invariant_6d_h192.log 2>&1

# ③ 용량 확대 (hidden_dim 128→256)
CUDA_VISIBLE_DEVICES=3 python -u train_tsem.py \
  --config configs/tsem_sage_invariant_6d_h256.yaml --gpus 3 \
  > logs/tsem/train_invariant_6d_h256.log 2>&1
```

## 2. ④ W×encoder 그리드 (W20/W30, 신규 캐시 생성) ★★

```bash
git pull origin main

# W=10, transformer (6D invariant GRU 기준과 encoder_type만 다른 짝)
python -u train_tsem.py --config configs/tsem_sage_invariant_6d_w10_transformer.yaml --gpus 0 \
  > logs/tsem/train_invariant_6d_w10_transformer.log 2>&1

# W=20, gru / transformer
python -u train_tsem.py --config configs/tsem_sage_invariant_6d_w20_gru.yaml --gpus 1 \
  > logs/tsem/train_invariant_6d_w20_gru.log 2>&1
python -u train_tsem.py --config configs/tsem_sage_invariant_6d_w20_transformer.yaml --gpus 2 \
  > logs/tsem/train_invariant_6d_w20_transformer.log 2>&1

# W=30, gru / transformer
python -u train_tsem.py --config configs/tsem_sage_invariant_6d_w30_gru.yaml --gpus 3 \
  > logs/tsem/train_invariant_6d_w30_gru.log 2>&1
python -u train_tsem.py --config configs/tsem_sage_invariant_6d_w30_transformer.yaml --gpus 0 \
  > logs/tsem/train_invariant_6d_w30_transformer.log 2>&1
```

## 3. ①+②③④ 조합 실험 (①의 8D를 기본 입력으로) ★

①(cos_rot,sin_rot 2채널 추가)이 만드는 새 신호가 ②③④ 각각의 효과와 누적되는지 보는
후속 실험 — 1·2번 결과가 유의미한 조합부터 우선 진행해도 된다.

```bash
git pull origin main

# ①+② (8D + 학습형 잔차 4D = 12D)
python -u train_tsem.py --config configs/tsem_sage_invariant_8d_rot_learnable.yaml --gpus 1 \
  > logs/tsem/train_invariant_8d_rot_learnable.log 2>&1

# ①+③ (8D, hidden_dim 192 / 256)
python -u train_tsem.py --config configs/tsem_sage_invariant_8d_rot_h192.yaml --gpus 2 \
  > logs/tsem/train_invariant_8d_rot_h192.log 2>&1
python -u train_tsem.py --config configs/tsem_sage_invariant_8d_rot_h256.yaml --gpus 3 \
  > logs/tsem/train_invariant_8d_rot_h256.log 2>&1

# ①+④ (8D, W×encoder 그리드)
python -u train_tsem.py --config configs/tsem_sage_invariant_8d_rot_w10_transformer.yaml --gpus 0 \
  > logs/tsem/train_invariant_8d_rot_w10_transformer.log 2>&1
python -u train_tsem.py --config configs/tsem_sage_invariant_8d_rot_w20_gru.yaml --gpus 1 \
  > logs/tsem/train_invariant_8d_rot_w20_gru.log 2>&1
python -u train_tsem.py --config configs/tsem_sage_invariant_8d_rot_w20_transformer.yaml --gpus 2 \
  > logs/tsem/train_invariant_8d_rot_w20_transformer.log 2>&1
python -u train_tsem.py --config configs/tsem_sage_invariant_8d_rot_w30_gru.yaml --gpus 3 \
  > logs/tsem/train_invariant_8d_rot_w30_gru.log 2>&1
python -u train_tsem.py --config configs/tsem_sage_invariant_8d_rot_w30_transformer.yaml --gpus 0 \
  > logs/tsem/train_invariant_8d_rot_w30_transformer.log 2>&1
```

> `--gpus`는 이 서버의 빈 GPU 번호로 조정. 위 번호는 예시일 뿐 실제 점유 상태를 `nvidia-smi`로
> 확인 후 배정할 것. 파라미터 규모가 크지 않아(최대 675K) 1 config당 GPU 1개면 충분.

## 4. ①+②+③ 삼중 조합 — ⏳ 조건부, ②단독 결과 확인 후에만 실행

②(학습형 잔차)가 위 1번에서 유의미한 개선을 보일 때만 실행한다. **②단독 결과가 6D
invariant(76.21%/74.78%) 대비 뚜렷한 개선이 없으면 이 두 config는 실행하지 말 것** —
효과 없는 조치를 삼중 조합에 섞으면 무엇이 기여했는지 해석할 수 없다(§하네스 원칙
"한 번에 하나씩만 바꿔 격리").

```bash
git pull origin main

python -u train_tsem.py --config configs/tsem_sage_invariant_8d_rot_learnable_h192.yaml --gpus 2 \
  > logs/tsem/train_invariant_8d_rot_learnable_h192.log 2>&1
python -u train_tsem.py --config configs/tsem_sage_invariant_8d_rot_learnable_h256.yaml --gpus 3 \
  > logs/tsem/train_invariant_8d_rot_learnable_h256.log 2>&1
```

스모크테스트(랜덤 텐서 forward+backward+NaN 확인)는 이미 통과했다 — params 388,027 / 681,147.

---

## 결과 공유 방법

각 실험 완료 후 아래 두 가지를 공유:
1. `checkpoints/tsem/<experiment>/results.json` — test acc/macro-F1/클래스별 지표/혼동행렬/
   Persist baseline 자동 포함
2. `logs/tsem/train_*.log`의 마지막 ~30줄 (best epoch, early stop 시점 확인용)

**비교 기준**:
| 모델 | test acc | test macro-F1 |
|---|---|---|
| Persist baseline | 63.17% | 50.55% |
| CRAT-Pred | 75.02% | 72.84% |
| **6D invariant (기준선, 로컬 완료)** | **76.21%** | **74.78%** |
| HiVT | 77.48% | 76.35% |
| QCNet | 77.97% | 76.96% |
| 10D 최종모델(10차-2, 비교 상한) | 81.91~81.97% | 81.95~82.03% |

①②③④ 각 단독/조합 실험이 6D invariant(76.21%/74.78%)를 얼마나 넘어서는지, 특히 HiVT
(77.48%)·QCNet(77.97%)을 넘어서는 조합이 있는지가 핵심 관찰 포인트.

## 실행 우선순위 요약

| 실험군 | 개수 | 우선순위 | 비고 |
|---|---|---|---|
| ①②③ 단독 (W=10) | 4 | ★★★ | 캐시 공유, 리스크 낮음(①③은 구조 변경 없음) |
| ④ W×encoder 그리드 | 5 | ★★ | W20/W30는 신규 캐시 생성(시간 더 걸림) |
| ①+②③④ 조합 | 8 | ★ | 1·2군 결과 보고 유의미한 것부터 진행해도 무방 |
| ①+②+③ 삼중 조합 | 2 | ⏳ 조건부 | §4 참조 — ②단독 결과 확인 전 실행 금지 |

이 서버에서 새로 실행할 config는 총 **19개**(①②③ 단독 4 + ④ W×encoder 그리드 5 + ①+②③④
조합 8 + ①+②+③ 삼중 조합 2, 마지막 2개는 조건부) — 6D invariant GRU(W=10) 기준선 자체는
로컬에서 이미 완료돼 재실행 불필요. 상세 배경·가설은 `docs/TSEM_journal_design.html`
§실험현황 "20260712 추가 실험계획" 참조.

---

## 실행 기록 — §1 ①②③ 단독 (2026-07-12 21:00 시작, 로컬 서버 oem-System-Product-Name)

> 이 서버(GPU 0-3 전부 프리, RTX 3090 24GB×4)에서 §1의 4개를 GPU당 1개씩 배정해 실행 중.
> 문서 명령의 `--gpus N`은 **train_tsem.py argparse에 없어 제거**(붙이면 "unrecognized
> arguments"로 즉사). 대신 `CUDA_VISIBLE_DEVICES=N`으로 각 job을 1장에 고정 —
> train_tsem.py가 `device_count>1`이면 자동 `nn.DataParallel`을 걸어, 고정 안 하면 각 job이
> 4장 전부를 물어 충돌하기 때문. config의 `data_dir`은 이 서버에 맞춰 `/home/oem/yklee/data/`로
> 로컬 수정(커밋 안 함). 캐시는 4개가 동일 키(`58c806f17c`) 공유 → warm-hit
> (train 575,686 / val 119,901 / test 120,435), 콜드 빌드 레이스 회피 위해 GPU0 먼저 띄운 뒤
> 나머지 3개 순차 기동.

| GPU | 실험 | config | params | 메모리(24GB 중) | 로그 |
|---|---|---|---|---|---|
| 0 | ① 8D rot | `tsem_sage_invariant_8d_rot.yaml` | 173,619 | ~16.1 GB | `logs/tsem/train_invariant_8d_rot.log` |
| 1 | ② learnable 잔차 4D | `tsem_sage_invariant_6d_learnable.yaml` | 175,281 | ~20.7 GB | `logs/tsem/train_invariant_6d_learnable.log` |
| 2 | ③ hidden 192 | `tsem_sage_invariant_6d_h192.yaml` | 380,969 | ~23.8 GB | `logs/tsem/train_invariant_6d_h192.log` |
| 3 | ③ hidden 256 | `tsem_sage_invariant_6d_h256.yaml` | 671,785 | ~15.5 GB | `logs/tsem/train_invariant_6d_h256.log` |

**메모리 검증 완료**: 4개 모두 Epoch 1(검증 포함, 최대 할당 구간)을 OOM 없이 통과. GPU2(h192)가
23.8GB로 빠듯하나 안정 상태(누수 아님). 한 GPU에 한 job이면 안전.

**프로토콜**: 300 epoch, early stop patience=50, ~78–94s/epoch. 예상 소요 최대 ~6.7h(early
stop 시 단축). 완료 후 각 `checkpoints/tsem/<experiment>/results.json`에 결과 저장.

### 최종 결과 (완료 후 기입)

| 실험 | best epoch | test acc | test macro-F1 | vs 6D invariant(76.21/74.78) |
|---|---|---|---|---|
| ① 8D rot | ⏳ | ⏳ | ⏳ | ⏳ |
| ② learnable 4D | ⏳ | ⏳ | ⏳ | ⏳ |
| ③ h192 | ⏳ | ⏳ | ⏳ | ⏳ |
| ③ h256 | ⏳ | ⏳ | ⏳ | ⏳ |

---

## 실행 기록 — §3 ①과의 조합 2개 (2026-07-12, 172.30.1.20 서버 GPU2/3)

> §1(①②③ 단독 4개, GPU0-3)과는 별도로, §3 "①+②③④ 조합" 중 ①+②·①+③(h192) 2개를
> **172.30.1.20 서버**(이 저장소가 실제로 위치한 서버, `conda activate tna_research`)
> GPU2/3에 추가로 배정해 실행 중. `CUDA_VISIBLE_DEVICES=N`으로 GPU 1장씩 고정(§1과 동일 이유 —
> 고정 안 하면 `device_count>1` 자동 `nn.DataParallel`로 다른 GPU와 충돌).

| GPU | 실험 | config | 로그 |
|---|---|---|---|
| 2 | ①+② (8D+학습형잔차4D=12D) | `tsem_sage_invariant_8d_rot_learnable.yaml` | `logs/tsem/train_invariant_8d_rot_learnable.log` |
| 3 | ①+③ (8D, hidden_dim 192) | `tsem_sage_invariant_8d_rot_h192.yaml` | `logs/tsem/train_invariant_8d_rot_h192.log` |

스모크테스트(랜덤 텐서 forward+backward+NaN)는 이미 PASS 상태(params 176,827 / 383,283) — 바로
본 실행 투입. 완료 시 `checkpoints/tsem/<experiment>/results.json` + 로그 tail을 이 절과
`docs/TSEM_journal_design.html` §20260712 추가 실험계획에 반영 예정.

**추가 — ①+③(h256), GPU0 (2026-07-12, 172.30.1.20 서버)**: GPU2/3(위 표)이 이미 h192/①+②로
찬 상태에서, GPU0이 비어 있어 `tsem_sage_invariant_8d_rot_h256.yaml`(params 674,867)을 추가
배정. 캐시(`a165448538`) warm-load, Epoch 1 완료 — `loss=0.6601 val_acc=0.6127 macro_f1=0.5756
LC_recall=0.8602 (83.4s)`, GPU 메모리 15.3GB/24GB로 **OOM 없이 안전** 확인. 로그:
`logs/tsem/train_invariant_8d_rot_h256.log`.

| GPU | 실험 | config | params | 메모리(24GB 중) | 로그 |
|---|---|---|---|---|---|
| 2 | ①+② (8D+학습형잔차4D=12D) | `tsem_sage_invariant_8d_rot_learnable.yaml` | 176,827 | — | `logs/tsem/train_invariant_8d_rot_learnable.log` |
| 3 | ①+③ (8D, hidden_dim 192) | `tsem_sage_invariant_8d_rot_h192.yaml` | 383,283 | — | `logs/tsem/train_invariant_8d_rot_h192.log` |
| 0 | ①+③ (8D, hidden_dim 256) | `tsem_sage_invariant_8d_rot_h256.yaml` | 674,867 | ~15.3GB(Epoch1 기준, 안전) | `logs/tsem/train_invariant_8d_rot_h256.log` |

### 최종 결과 (2026-07-13 완료)

| 실험 | best epoch | test acc | test macro-F1 | vs 6D invariant(76.21/74.78) |
|---|---|---|---|---|
| ①+② learnable | 185 (235 중단) | **76.37%** | **75.13%** | +0.16%p / +0.35%p |
| ①+③ h192 | 120 (170 중단) | **76.57%** | **75.27%** | +0.36%p / +0.49%p |
| ①+③ h256 | 76 (126 중단) | **76.51%** | **75.02%** | +0.30%p / +0.24%p |

3개 다 6D invariant 대비 개선은 소폭(≤0.5%p)에 그쳐, HiVT(77.48%/76.35%)·QCNet(77.97%/76.96%)
격차는 여전히 못 메웠다. `docs/TSEM_journal_design.html` §20260712 추가 실험계획 표 갱신 완료.

**참고 — position-only 신경망도 같은 시점 완료(별도 GPU1, §위치 암기 검증 트랙)**: 300epoch 완주
(best@268) — test acc **81.39%** / macro-F1 **81.70%**, 10D 최종모델(81.91~81.97%)과 0.5%p
이내로 수렴. 상세: `docs/TSEM_journal_design.html` §위치 암기 검증①·④ 종합.

---

## 예약 — §3 ①+④ W×encoder 조합 4개 (2026-07-12 22:23 예약, 172.30.1.20 서버)

> 위 §3 세 실험(GPU0/2/3)이 각 GPU를 여전히 점유 중이라, "①+④(8D, W×encoder 그리드)" 조합
> 4개(`tsem_sage_invariant_8d_rot_w10_transformer.yaml`, `_w20_gru.yaml`, `_w20_transformer.yaml`,
> `_w30_gru.yaml`)를 **PID 감시 런처**(`scratchpad/queue_s3_w_grid.sh`, PID 2509557 백그라운드)로
> 예약해뒀다 — 각 GPU의 현재 job이 끝나는 즉시(메인 PID 종료 확인 + 15초 여유) 해당 GPU에
> 자동으로 다음 job을 투입한다. 상태 로그: `logs/tsem/s3_wgrid_status.txt`.

| GPU | 현재 실행 중(종료 대기 PID) | 예약된 실험 | config | 비고 |
|---|---|---|---|---|
| 0 | ①+③ h256 (PID 2495409) | ①+④ W10-transformer | `tsem_sage_invariant_8d_rot_w10_transformer.yaml` | §1 W10 캐시(`a165448538`) 재사용, 레이스 없음 |
| 1 | position_only (PID 2200751) | ①+④ W20-gru | `tsem_sage_invariant_8d_rot_w20_gru.yaml` | 신규 W20 캐시 최초 생성(~10-15분) |
| 2 | ①+② learnable (PID 2461057) | ①+④ W20-transformer | `tsem_sage_invariant_8d_rot_w20_transformer.yaml` | GPU 확보 **+** W20-gru 로그에 `Epoch 001` 찍힐 때까지 대기(캐시 키 동일 → 콜드빌드 레이스 방지) |
| 3 | ①+③ h192 (PID 2461112) | ①+④ W30-gru | `tsem_sage_invariant_8d_rot_w30_gru.yaml` | 신규 W30 캐시 최초 생성, 단독이라 레이스 없음 |

완료 시 각 `checkpoints/tsem/<experiment>/results.json` + 로그 tail을 이 절과
`docs/TSEM_journal_design.html` §20260712 추가 실험계획에 반영 예정.

### 결과 — 4개 중 3개 크래시 (2026-07-13)

런처가 각 GPU를 확보해 순서대로 투입했으나(GPU1 00:06, GPU0 00:51, GPU3 01:45), 캐시 빌드까지는
전부 성공(`cache/tsem/*_W20_*`, `*_W30_*` 생성 확인)하고 **Epoch 1 진입 전에 3개가 크래시**했다.

| 실험 | GPU | 결과 | 원인 |
|---|---|---|---|
| W10-transformer | 0 | ❌ 크래시 | `RuntimeError: CUDA error: invalid configuration argument` — `nn.TransformerEncoder`의 `scaled_dot_product_attention` 커널 호출에서 발생. 스모크테스트(랜덤 소배치)는 PASS했었으나 실배치 규모에서 재현 — 원인 미확인 |
| W20-gru | 1 | ❌ 크래시 | `torch.OutOfMemoryError` — backward에서 8.71GiB 추가 요청 실패(forward만으로 이미 15.4GB 점유, GPU 총 23.57GB). §2 문서의 사전 예측("~24GB 근처 ⚠️")이 실측으로 확인된 셈 |
| W20-transformer | 2 | 🔄 실행 중 | W20-gru가 크래시해 "`Epoch 001` 로그까지 대기" 조건이 영원히 성립하지 않게 됨 → PID 감시 서브셸을 kill하고, 이미 완성된 W20 캐시를 이용해 수동으로 투입(2026-07-13 09:xx) |
| W30-gru | 3 | ❌ 크래시 | `RuntimeError: CUDA error: an illegal memory access was encountered` — `models/et_nagraphsage.py`의 2-hop 레이어(`layer_2hop`) forward 중 `lin_gate` 호출에서 발생. OOM 메시지가 아니라 **실제 인덱싱/텐서 크기 버그일 가능성** — W=30에서만 나타나는지 코드 조사 필요 |

**재시도 전 확인 필요**: (1) W20-gru는 `batch_size` 축소 또는 gradient checkpointing으로 재시도,
(2) W30-gru는 OOM이 아니므로 `layer_2hop`이 W=30 시퀀스에서 인덱스 범위를 벗어나는지 코드 리뷰
선행, (3) W10-transformer의 커널 오류는 PyTorch/CUDA 버전 조합 이슈일 가능성 —
`TORCH_USE_CUDA_DSA`로 재현 재시도 권장. 셋 다 아직 미착수 상태.

---

## 예약 — §2 ④ W×encoder 그리드 (2026-07-12 21:15 예약, §1 종료 후 자동 실행)

메모리 인지형 런처(`scratchpad/run_s2_grid.sh`, PID 백그라운드)를 걸어둠. §1이 GPU를 비우면
(free≥21GB) 아래 **우선순위(메모리 오름차순)**로 자동 투입하며, 각 config를 단일 GPU로 띄워
**Epoch 1 통과=메모리 fit**을 확인한다. `out of memory`가 뜨면 kill 후 그 GPU를 다음 config에
양보하고, 해당 config를 **§2-deferred(다중 GPU 후보)**로 기록한다.

**메모리 예측 근거**: §1의 W10-gru가 ~16GB. W가 커지면 시퀀스 활성값이 늘고(GRU ~선형),
transformer는 attention이 O(W²)라 같은 W에서 더 무겁다. → 초과 위험 오름차순.

**캐시**: W10-transformer는 §1의 W10 캐시(`58c806f17c`, warm) 재사용. W20/W30은 각각 신규
캐시를 1회 빌드(~10-15분). pair(gru/transformer)가 같은 캐시 키를 공유하므로 **gru를 먼저
Epoch1까지 도달시킨 뒤 transformer를 띄워** 콜드 빌드 레이스를 방지한다(런처가 순차 보장).

| 순위 | 실험 | config | W/encoder | 캐시 | 예상 단일GPU 메모리 |
|---|---|---|---|---|---|
| 1 | W10-transformer | `..._6d_w10_transformer.yaml` | 10/tf | warm(재사용) | ~16–18GB ✅ 안전 |
| 2 | W20-gru | `..._6d_w20_gru.yaml` | 20/gru | 신규 빌드 | ~24GB 근처 ⚠️ |
| 3 | W20-transformer | `..._6d_w20_transformer.yaml` | 20/tf | warm(2번이 빌드) | 24GB 경계 ⚠️ |
| 4 | W30-gru | `..._6d_w30_gru.yaml` | 30/gru | 신규 빌드 | 24GB 초과 가능 ⛔ |
| 5 | W30-transformer | `..._6d_w30_transformer.yaml` | 30/tf | warm(4번이 빌드) | 24GB 초과 유력 ⛔ |

> 예측은 투입 순서용일 뿐, **실제 fit/초과 판정은 런처가 Epoch1/OOM으로 실측**한다.

**초과 config 처리(다중 GPU, 미뤄둠)**: 단일 24GB를 넘는 config는 `logs/tsem/s2_deferred.txt`에
아래 형태로 기록된다 — train_tsem.py는 `device_count>1`이면 자동 `nn.DataParallel`로 배치를
분할하므로 GPU 2장을 주면 batch 4096→2048/GPU로 쪼개져 실행 가능:
```bash
CUDA_VISIBLE_DEVICES=a,b python -u train_tsem.py --config configs/<cfg>.yaml > logs/tsem/<cfg>.log 2>&1 &
```
GPU 여유가 생기면 deferred 목록을 2장씩 묶어 실행한다. (실행 로그: `logs/tsem/s2_status.txt`)

### §2 결과 (완료 후 기입)

| 실험 | 판정 | best epoch | test acc | test macro-F1 |
|---|---|---|---|---|
| W10-transformer | ⏳ | ⏳ | ⏳ | ⏳ |
| W20-gru | ⏳ | ⏳ | ⏳ | ⏳ |
| W20-transformer | ⏳ | ⏳ | ⏳ | ⏳ |
| W30-gru | ⏳ | ⏳ | ⏳ | ⏳ |
| W30-transformer | ⏳ | ⏳ | ⏳ | ⏳ |
